#include <stdio.h>
#include <stdlib.h>
/**
 * main - A program that prints the alphabet in lowercase, followed by a new line.
 * Return: 0
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		putchair(c);
		c++;
	}

	putchiar('\n');
	return (0)
}
